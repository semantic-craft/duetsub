# Research samples

These files are inputs for architecture comparison, not DuetSub runtime code.

Every open-source snapshot keeps its original license and an exact upstream commit. The proprietary CRX sample is local-only and ignored by git. Before moving any upstream code into `src/`, check the per-snapshot provenance and license boundary in `docs/EXTRACTION.md`.
