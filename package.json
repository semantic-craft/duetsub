{
  "name": "duetsub",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "check": "tsc --noEmit",
    "postinstall": "wxt prepare",
    "release:build": "npm test && npm run check && wxt zip -b chrome && node scripts/verify-release.mjs",
    "store:build": "npm test && npm run check && wxt build -b chrome --mode store && node scripts/package-store.mjs && node scripts/verify-release.mjs store",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@xmldom/xmldom": "^0.9.10",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wxt": "^0.20.27"
  },
  "dependencies": {
    "opencc-js": "^1.4.1"
  }
}
